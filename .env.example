# Brightdata MCP Server Environment Variables
# Copy this file to .env and fill in your values for local development

# Required: Your Brightdata API token (must have Admin permissions)
# Get from: https://brightdata.com/cp/setting/users
API_TOKEN=your-brightdata-api-token-here

# Optional: Custom Web Unlocker zone name (defaults to 'mcp_unlocker')
# WEB_UNLOCKER_ZONE=my-custom-zone

# Optional: Browser automation zone name (defaults to 'mcp_browser')
# Only needed if you want to use browser automation tools
# BROWSER_ZONE=my-browser-zone

# Optional: Rate limiting (format: limit/time+unit, e.g., 100/1h, 50/30m, 10/5s)
# RATE_LIMIT=100/1h

# Railway automatically sets these variables:
# PORT=8000 (or dynamic port)
# RAILWAY_STATIC_URL=your-deployment-url.up.railway.app